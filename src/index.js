import React from 'react';
import ReactDOM from 'react-dom';
import App from './Apps/1/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
